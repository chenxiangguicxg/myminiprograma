<!--pages/runList/runList.wxml-->
<view class="runTypeContainer" bindtap="_handleTap">
  <block wx:for="{{runItems}}" wx:key="id">
    <x-runItem id="{{item.id}}" runTypeItem="{{item}}"/> 
  </block>
</view>

